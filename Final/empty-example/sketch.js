

function setup() {
createCanvas(windowWidth,windowHeight);
  for (var i = 0; i < 1; i++) {
     var size = random(5, 10);
    // var acc = map(size, 5, 10, 0.1, 0.05);
    Gsqs.push( new Gsqs)
}

}


function draw() {

  push();
  background(0);

  pop();



    for (var i = 0; i < Gsqs.length; i++) {
     Gsqs[i].display()

}



}
